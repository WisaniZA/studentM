// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

// environment.ts
export const environment = {
  production: false,
  baseUrl: 'http://localhost:8080/api/v1',
  version: '2.0.3'
 
};

/*  
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

// update format time  version: '1.4.0'
// update callDuration from using the SDK one to creating our own 2.0.0' DTMF &TRANSFER 
// update callDuration from using the SDK one to creating our own 1.5.0
// updated the spelling error on call/recieve call page  1.6.0
// log them our when they are on AOD Form 1.7.0

